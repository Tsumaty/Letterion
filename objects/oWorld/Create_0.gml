globalvar lettersCount, coinsCount, levelComplete;
lettersCount = 0;
coinsCount = 0;
levelComplete = false;

if (room == rLevelAlphabet)
{
    textX = window_get_width() / 2 - 1;
    textY = window_get_height() - 50;
}